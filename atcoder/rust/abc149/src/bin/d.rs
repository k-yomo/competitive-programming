#![allow(unused_imports)]
use proconio::*;
use proconio::marker::*;
use std::cmp::*;
use std::collections::*;
use std::io::Write;
use std::ops::Bound::*;
use itertools::*;

fn main() { 
    input! {
        n: usize, k: usize,
        r: usize, s: usize, p: usize,
        t: Chars,
    }

    r_dp = vec![];
    s_dp = vec![];
    p_dp = vec![];
    for hand in t {
    }
}
