#![allow(unused_imports)]
use proconio::*;
use proconio::marker::*;
use std::cmp::*;
use std::collections::*;
use std::io::Write;
use std::ops::Bound::*;
use itertools::*;

fn main() { 
    input! {
       n: i64,
       k: i64
    }

    // 2 1
    // a + b - c - d = k
    // b - c - d = k - a
    for i in 1..n+1 {
        let rest = k - i;
        for  in  {
            
        }
    }
}
