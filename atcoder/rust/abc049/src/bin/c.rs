#![allow(unused_imports)]
use proconio::*;
use std::cmp::*;
use std::collections::*;
use std::io::Write;
use std::ops::Bound::*;
 
fn main() { 
    input! {
        s: String,
    }
    
    if s.len() < 5 {
        return println!("NO");
    }
    
    let mut t = "";
    while  {
        
    }
}
